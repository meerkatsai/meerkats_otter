# Copy to .env for local runs (do not commit real secrets).
# Neon app_writer connection string (INSERT+SELECT role, not the owner):
DATABASE_URL=postgresql://app_writer:PASSWORD@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require
PORT=8080
